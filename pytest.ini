[pytest]
# tests/ holds the suite; the repo root goes on sys.path so `import server`
# works from inside it. Without pythonpath, pytest would only put tests/
# itself on the path and every module would fail to import the thing it is
# testing.
testpaths = tests
pythonpath = .
